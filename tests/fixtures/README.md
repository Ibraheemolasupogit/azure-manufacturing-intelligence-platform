# Test Fixtures

This directory is reserved for small deterministic fixtures used by future tests.

Milestone 1 tests do not require generated manufacturing datasets. Future fixtures must remain synthetic, minimal, and safe to commit.
