# Limitations

The assistant uses deterministic rules, not a real LLM. It can summarize and cite governed evidence, but it cannot infer facts absent from the evidence, perform live retrieval, execute production actions, generate dashboards, deploy Azure AI services, or inspect real-world operations.

Milestone 10 dashboard outputs are deliberately deferred.
