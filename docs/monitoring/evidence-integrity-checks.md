# Evidence Integrity Checks

Evidence integrity checks compare configured manifests with the files on disk. For each artifact, monitoring records path, expected and actual row count where applicable, expected and actual file size, expected and actual SHA-256 hash, and pass/fail status.

The controlled run checked 62 artifacts and produced a manifest integrity score of 100.
