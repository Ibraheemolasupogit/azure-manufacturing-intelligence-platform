# Semantic Model

`outputs/dashboard/semantic_model.json` describes local semantic metadata: model name, tables, columns, data types, primary keys, supported relationships, measures, display folders, calculation notes, limitations, and synthetic-data disclaimer.

Relationships are limited to keys that exist in generated tables. No Fabric semantic model is deployed.
