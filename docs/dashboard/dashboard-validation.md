# Dashboard Validation

Existing-run validation checks required outputs, row counts, file sizes, hashes, non-empty tables, primary key uniqueness, semantic model references, relationship columns, page specs, visual specs, portfolio docs, synthetic disclaimers, deployment flags, lineage targets, upstream hashes, safe relative paths, and deterministic run identity.

Tampering with output files or metadata must fail validation.
