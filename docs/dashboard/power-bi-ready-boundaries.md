# Power BI-Ready Boundaries

The outputs are local import-ready artefacts that conceptually map to Microsoft Power BI, Fabric semantic models, Azure Synapse serving layers, ADLS Gen2 curated gold data, Microsoft Purview lineage, and Azure Monitor views.

No `.pbix`, Power BI Service dataset, Fabric semantic model, API call, credential, refresh schedule, workspace, or Azure resource is created.
